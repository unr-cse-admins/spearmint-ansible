- name: apply common configuration to all nodes
  hosts: all
  remote_user: root
  roles:
    - common

- name: apply configuration to compute nodes
  hosts: compute
  remote_user: root
  roles:
    - compute